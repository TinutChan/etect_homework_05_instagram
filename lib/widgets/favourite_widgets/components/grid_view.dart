import 'package:flutter/material.dart';

class GridViewsWidgets extends StatelessWidget {
  final String? images;
  const GridViewsWidgets({Key? key, this.images}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
